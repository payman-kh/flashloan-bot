# Make sure you're in the contracts directory
cd contracts

# Compile the contracts
npx hardhat compile

# Start a local hardhat node with mainnet fork
npx hardhat node

# In a new terminal, still in contracts directory, deploy the contract
npx hardhat run scripts/deploy.js --network hardhat